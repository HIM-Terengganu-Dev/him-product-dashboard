# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Explicitly declare text files you want to always normalize to LF line endings.
*.{js,jsx,ts,tsx,css,md,json,html} eol=lf